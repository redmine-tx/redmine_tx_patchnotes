# CLAUDE.md

이 파일은 Claude AI가 redmine_tx_patchnotes 플러그인을 개발할 때 참고할 가이드를 제공합니다.

## 플러그인 개요

`redmine_tx_patchnotes`는 Redmine의 특정 버전(Version)에 속한 일감들 중 패치노트 작성 대상을 분류하고, 패치노트 작성 여부를 추적하여 누락을 방지하는 플러그인입니다.

### 의존성

이 플러그인은 다음 플러그인들을 필요로 합니다:
- `redmine_tx_advanced_issue_status` - 이슈 상태 Stage 시스템
- `redmine_tx_advanced_tracker` - Tracker 확장 시스템

선택적 의존성:
- `redmineup_tags` - 태그 기반 예외 처리 기능

## 주요 기능

### 1. 패치노트 작성 헤더 시스템

일감의 설명(description) 또는 코멘트(journal notes)에 특정 헤더를 포함하여 패치노트를 작성합니다.

```
PATCH_NOTE
- 새로운 기능이 추가되었습니다
- 버그가 수정되었습니다
```

**파트(Part) 번호 지정:**
```
PATCH_NOTE#2
- 두 번째 파트의 패치노트 내용
```

헤더 뒤의 `#숫자`로 패치노트를 여러 파트로 분류할 수 있습니다. 숫자를 생략하면 자동으로 파트 1로 간주됩니다.

### 2. 패치노트 작성 예외 처리

특정 일감은 패치노트 작성이 필요 없을 수 있습니다. 다음과 같이 예외 처리할 수 있습니다:

**코멘트에 스킵 헤더 포함:**
```
NO_PATCH_NOTE
이 일감은 내부 작업이므로 패치노트 작성이 필요 없습니다.
```

**태그 기반 예외 처리:**
- `redmineup_tags` 플러그인이 설치된 경우
- 설정에서 지정한 태그(기본값: NoQA)가 달린 일감은 자동으로 예외 처리됩니다

### 3. 일감 필터링 규칙

플러그인은 다음 규칙에 따라 일감을 필터링합니다:

#### 포함 규칙
- 선택한 버전(Version)의 fixed_version_id를 가진 일감
- 설정에서 지정한 Tracker 유형(e_tracker_on)에 속한 일감만 포함
- 폐기되지 않은 일감 (`IssueStatus.discarded_ids` 제외)

#### 제외 규칙
- 설정에서 지정한 제외 Tracker 유형(e_tracker_off)에 속한 일감
- 제외 Tracker를 상위 부모로 가진 모든 하위 일감도 자동 제외
- 패치노트 스킵 헤더가 포함된 일감
- 패치노트 예외 태그가 달린 일감

### 4. 일감 분류

필터링된 일감들은 다음과 같이 분류됩니다:

1. **패치노트 작성 완료 일감** (`@issues_patch_note_complete`)
   - PATCH_NOTE 헤더가 포함된 패치노트가 작성된 일감

2. **패치노트 작성 예외 일감** (`@no_patch_note_issues`)
   - NO_PATCH_NOTE 헤더가 있거나 예외 태그가 달린 일감
   - 패치노트 작성이 필요 없는 일감

3. **패치노트 누락 일감** (`@issues_need_patch_notes`)
   - 패치노트가 없고, 완료 상태가 아닌 일감
   - 작업 중이거나 검토 중인데 패치노트를 아직 작성하지 않은 일감

4. **패치노트 없이 완료 처리된 일감** (`@issues_complete_without_patch_notes`)
   - 완료 상태(설정의 complete)로 변경되었으나 패치노트가 없는 일감
   - 주의가 필요한 일감 목록

### 5. 공개용 vs 내부 참조용 패치노트

일부 패치노트는 운영자들만 참조하기 위한 용도로, 유저 공개용 패치노트에는 포함되지 않아야 할 수 있습니다.

**설정 방법:**
1. Boolean 타입의 커스텀 필드 생성 (예: "패치노트 미포함")
2. 플러그인 설정에서 "패치노트 미공개용 커스텀 필드" 선택
3. 내부용 패치노트가 필요한 일감의 해당 필드를 체크

**표시 방식:**
- 공개용 패치노트: 상단에 "📢 공개용 패치노트" 섹션으로 표시
- 내부용 패치노트: 하단에 "🔒 내부 참조용 패치노트 (운영자 전용)" 섹션으로 분리 표시
- 각각 별도의 "클립보드에 복사하기" 버튼 제공

### 6. 패치노트 표시 및 정렬

패치노트는 파트(Part) 번호별로 그룹화되어 표시됩니다.

각 파트 내에서는 설정한 Tracker 순서에 따라 정렬됩니다:
- 설정에서 지정한 `tracker_order`에 따라 정렬
- 순서가 지정되지 않은 Tracker는 맨 뒤에 표시

### 7. 클립보드 복사 기능

각 파트의 패치노트를 HTML 형식으로 클립보드에 복사할 수 있습니다.

**특징:**
- Textile 형식이 HTML로 렌더링되어 복사됨
- 이미지는 데이터 URL로 변환되어 복사 (외부 참조 없이 독립적으로 사용 가능)
- 일감 제목, 번호, 담당자 정보 포함
- 외부 문서(워드, 노션 등)에 바로 붙여넣기 가능
- 공개용과 내부용 패치노트를 각각 별도로 복사 가능

## 설정 항목

플러그인 설정은 관리자 > 플러그인 > Redmine Tx Patchnotes 에서 변경할 수 있습니다.

### 패치노트 작성이 필요한 일감 유형 (e_tracker_on)
- 패치노트 작성 대상이 되는 Tracker를 선택합니다
- 예: 기능, 개선, 버그 등

### 패치노트 작성에서 제외할 일감 유형 (e_tracker_off)
- 패치노트 대상에서 제외할 Tracker를 선택합니다
- 이 유형의 일감을 상위 부모로 가진 모든 하위 일감도 자동으로 제외됩니다
- 예: 지원업무, 내부작업 등

### 패치노트가 작성 완료된 일감의 상태 (complete)
- 어떤 상태를 "완료"로 간주할지 선택합니다
- 이 상태에 도달했는데 패치노트가 없으면 경고 목록에 표시됩니다
- 예: 종결, 완료, QA완료 등

### 패치노트 헤더 (patch_note_header)
- 기본값: `PATCH_NOTE`
- 일감 설명이나 코멘트에서 패치노트를 식별하는 문자열

### 패치노트 스킵 헤더 (patch_skip_header)
- 기본값: `NO_PATCH_NOTE`
- 패치노트 작성 예외 처리를 위한 문자열

### 패치노트 없어도 되는 태그 (patch_skip_tag)
- 기본값: `NoQA`
- `redmineup_tags` 플러그인이 설치된 경우, 이 태그가 달린 일감은 패치노트 작성 예외로 처리

### 패치노트 미공개용 커스텀 필드 (internal_note_custom_field)
- Boolean 타입의 커스텀 필드를 선택합니다
- 이 필드가 체크된 일감의 패치노트는 "내부 참조용 패치노트"로 분류됩니다
- 유저 공개용 패치노트와 별도로 표시되어 운영자만 참조할 수 있습니다
- 선택하지 않으면 모든 패치노트가 공개용으로 처리됩니다

### 패치노트 유형 순서 (tracker_order)
- 패치노트 표시 시 Tracker의 정렬 순서를 지정합니다
- 드래그 앤 드롭으로 순서를 변경할 수 있습니다

## 컨트롤러 API

### PatchnotesController

**액션:**
- `index` - 패치노트 목록 및 분류된 일감 표시

**주요 인스턴스 변수:**
```ruby
@versions                              # 최근 2개월 이내의 오픈된 버전 목록 (최대 10개)
@selected_version                      # 현재 선택된 버전
@notes                                 # 패치노트 정보 배열 [{ issue:, notes:, part: }]
@public_notes                          # 공개용 패치노트 배열
@internal_notes                        # 내부 참조용 패치노트 배열
@notes_by_part                         # 공개용 패치노트를 파트별로 그룹화 (하위 호환용)
@public_notes_by_part                  # 공개용 패치노트를 파트별로 그룹화
@internal_notes_by_part                # 내부용 패치노트를 파트별로 그룹화
@issues                                # 필터링된 전체 일감
@issues_patch_note_complete            # 패치노트 작성 완료 일감
@issues_need_patch_notes               # 패치노트 누락 일감
@issues_complete_without_patch_notes   # 패치노트 없이 완료된 일감
@no_patch_note_issues                  # 패치노트 작성 예외 일감
```

**파라미터:**
- `version_id` - 선택할 버전 ID
- `tab` - 활성 탭 번호 (1-5)

**권한:**
- `:view_patchnotes` - 패치노트 보기 권한 필요

## 사용 예제

### 개발자 워크플로우

1. 일감 작업 시작

2. 작업 완료 후 일감에 코멘트 추가:
   ```
   PATCH_NOTE
   - 사용자 로그인 기능이 개선되었습니다
   - 비밀번호 찾기 기능이 추가되었습니다
   ```

3. 내부 참조용 패치노트 작성 (운영자만 볼 내용):
   - 일감의 "패치노트 미포함" 커스텀 필드를 체크
   - 패치노트 작성:
     ```
     PATCH_NOTE
     - 내부 로그 수집 기능 개선
     - 관리자 대시보드 성능 최적화
     ```

4. 내부 작업이나 기술 부채 해소 등 패치노트가 필요 없는 경우:
   ```
   NO_PATCH_NOTE
   내부 리팩토링 작업으로 사용자에게 보이는 변경사항 없음
   ```
   또는 일감에 `NoQA` 태그 추가

5. 프로젝트 메뉴 > 패치노트 에서 누락된 일감 확인

### PM/QA 워크플로우

1. 버전 선택 (예: v1.2.0)

2. 각 탭에서 일감 상태 확인:
   - 패치노트 탭:
     - 공개용 패치노트 확인 및 복사 (유저 공개용 릴리즈 노트 작성용)
     - 내부 참조용 패치노트 확인 (운영자 전용, 유저 공개 불필요)
   - 누락 일감 탭: 패치노트 작성이 필요한 일감 확인
   - 완료 일감 탭: 패치노트 없이 완료된 일감 확인 (주의 필요)

3. 공개용 패치노트를 복사하여 유저용 릴리즈 노트 작성

4. 내부용 패치노트를 참조하여 운영팀 공지사항 작성

## 주의사항

### 패치노트 검색 우선순위

시스템은 다음 순서로 패치노트를 검색합니다:
1. 일감의 설명(description)에서 PATCH_NOTE 헤더 검색
2. 없으면 코멘트(journals)를 역순으로 검색하여 가장 최근의 PATCH_NOTE 헤더 사용

### Worker 필드 지원

뷰에서 `issue.worker_id` 필드를 참조합니다. 이는 `redmine_tx_0_base` 플러그인의 커스텀 필드로 추정됩니다.
- `worker_id`가 있으면 해당 사용자 이름 표시
- 없으면 `assigned_to` (담당자) 이름 표시

## 확장 가능성

### 커스텀 필터 추가

`patchnotes_controller.rb:18-20`에서 설정값을 읽어 필터링하므로, 새로운 필터 조건을 추가하려면:
1. `init.rb`의 settings에 새 설정 추가
2. `app/views/settings/_redmine_tx_patchnotes.html.erb`에 설정 UI 추가
3. `patchnotes_controller.rb`의 `index` 액션에서 필터링 로직 추가

### 패치노트 포맷 확장

`find_patch_note_info` 메소드를 수정하여 다양한 패치노트 포맷을 지원할 수 있습니다.

현재 지원 포맷:
- `PATCH_NOTE` - 파트 1로 자동 설정
- `PATCH_NOTE#2` - 파트 2로 설정
- `PATCH_NOTE#N` - 파트 N으로 설정
